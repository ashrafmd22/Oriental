export interface FormData {
  companyName: string;
  contactPerson: string;
  email: string;
  phone: string;
  quantity: string;
  budgetRange: string;
  deliveryDate: string;
  productRequirements: string;
}

export interface Errors {
  email: string;
  phone: string;
  companyName: string;
  contactPerson: string;
  quantity: string;
  budgetRange: string;
  deliveryDate: string;
  productRequirements: string;
}