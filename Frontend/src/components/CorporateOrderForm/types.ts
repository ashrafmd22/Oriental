export interface FormData {
  companyName: string;
  contactPerson: string;
  email: string;
  phone: string;
  productRequirements: string;
}

export interface Errors {
  email: string;
  phone: string;
  companyName: string;
  contactPerson: string;
  productRequirements: string;
}